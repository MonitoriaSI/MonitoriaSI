<%= form_with(model: discipline, local: true) do |form| %>
  <% if discipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(discipline.errors.count, "error") %> prohibited this discipline from being saved:</h2>

      <ul>
      <% discipline.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao, id: :discipline_descricao %>
  </div>

  <div class="field">
    <%= form.label :codigo %>
    <%= form.text_field :codigo, id: :discipline_codigo %>
  </div>

  <div class="field">
    <%= form.label :sigla %>
    <%= form.text_field :sigla, id: :discipline_sigla %>
  </div>

  <div class="field">
    <%= form.label :cargaHoraria %>
    <%= form.number_field :cargaHoraria, id: :discipline_cargaHoraria %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
